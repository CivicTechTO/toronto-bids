name: bids
channels:
  - conda-forge
  - defaults
dependencies:
  - lxml
  - pip
  - python>=3.12
  - pip:
      - azure-common
      - azure-core
      - azure-identity
      - azure-keyvault-secrets
      - azure-storage-file-share
      - beautifulsoup4
      - numpy
      - pandas
      - python-magic
      - requests
      - selenium
      - slack-sdk
      - tqdm
      - webdriver-manager
prefix: /home/alex/anaconda3/envs/bids
